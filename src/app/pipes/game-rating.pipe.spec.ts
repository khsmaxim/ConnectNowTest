import { GameRatingPipe } from './game-rating.pipe';

describe('GameRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new GameRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
